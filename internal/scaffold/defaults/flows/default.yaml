name: default
stages:
  - name: brainstorm
    agents: [{package: brainstorm}]
    workspace: worktree
    gate: auto
    artifacts: [brainstorm.md]
  - name: spec
    agents: [{package: spec-writer}]
    workspace: worktree
    gate: approve_artifact
    artifacts: [spec.md]
  - name: plan
    agents: [{package: planner}]
    workspace: worktree
    gate: plan_review
    artifacts: [plan.md, touchset.json]
  - name: execute
    agents: [{package: executor}]
    workspace: worktree
    gate: auto
    heavy_slot: true
    retries: 2
    verify_after_change: test_cmd
    artifacts: []
  - name: correctness-review
    agents: [{package: correctness-reviewer}]
    workspace: worktree
    gate: auto
    heavy_slot: true
    retries: 2
    verify_after_change: test_cmd
    artifacts: []
  - name: clean-code-review
    agents: [{package: clean-code-reviewer}]
    workspace: worktree
    gate: auto
    retries: 2
    verify_after_change: test_cmd
    artifacts: []
  - name: librarian
    agents: [{package: librarian}]
    workspace: worktree
    gate: auto
    retries: 2
    verify_after_change: test_cmd
    artifacts: []
  - name: merge-verification
    agents: [{package: merge-verifier}]
    workspace: worktree
    merge_barrier: true
    gate: auto
    heavy_slot: true
    artifacts: [merge-report.md, merge-decision.json, verification.json]
